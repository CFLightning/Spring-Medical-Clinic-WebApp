export declare class Patient {
    private _id;
    private _name;
    private _surname;
    constructor(ids?: number, names?: string, surnames?: string);
    id: number;
    name: string;
    surname: string;
}
